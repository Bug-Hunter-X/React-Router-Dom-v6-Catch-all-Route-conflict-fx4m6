# React Router Dom v6 Catch-all Route Conflict

This repository demonstrates a common issue encountered when using catch-all routes (`/*`) in React Router v6. The catch-all route unintentionally overrides other routes, resulting in unexpected navigation behavior.  The solution highlights the correct placement of catch-all routes within the route definitions to resolve this conflict.